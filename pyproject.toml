[project]
name = "pareto-optimization-visualizer"
version = "0.1.0"
description = "Multi-criteria permutation flow shop (m=3): Pareto SA + HVI and scalarized SA"
requires-python = ">=3.12"
dependencies = ["matplotlib==3.10.0"]
